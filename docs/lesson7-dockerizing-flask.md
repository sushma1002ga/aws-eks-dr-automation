# Dockerizing Flask Application

Created Dockerfile for Flask API.

Steps:
- Built Docker image
- Ran container on port 5000
- Verified API endpoints

Outcome:
Flask app now runs inside Docker container.
